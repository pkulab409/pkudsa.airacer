# AI Racer 测试维护清单

> 版本: v1.2
> 更新日期: 2026-05-01
> 项目: pkudsa.airacer

---

## 待解决问题清单

### 模块 A: 后端 - 代码提交与验证

#### 问题 A1: 缺少对禁用导入的 AST 静态检查测试
**优先级**: P1 (中)
**状态**: ⚠️ 部分解决 - `test_code_sandbox_escape.py` 中存在相关测试，但 `test_submission.py` 中没有。
**解决方案**: 在 `test_submission.py` 文件中添加 `test_validate_code_forbidden_imports()` 测试。

---

### 模块 B: 后端 - 管理员控制

#### 问题 B1: 缺少并发会话设置的竞争条件测试
**优先级**: P1 (中)
**状态**: ⚠️ 待办
**解决方案**: 在 `test_admin.py` 文件中添加 `test_concurrent_set_session()` 测试。

#### 问题 B2: 缺少无效状态转换的边界测试
**优先级**: P2 (低)
**状态**: ⚠️ 待办
**解决方案**: 在 `test_race_state_machine.py` 文件中添加 `test_invalid_transitions()` 测试。

---

### 模块 F: 后端 - 录像系统

#### 问题 F1: 缺少对损坏 JSONL 文件的容错测试
**优先级**: P1 (中)
**状态**: ⚠️ 待办
**解决方案**: 在 `test_recording.py` 文件中添加 `test_corrupted_telemetry_file()` 测试。

---

### 模块 I: 前端 - 单元测试

#### 问题 I1: 缺少前端单元测试
**优先级**: P2 (低)
**状态**: ⚠️ 待办
**解决方案**: 创建 `tests/frontend/unit/` 目录，并添加 JS 测试文件。

---

### 模块 J: 性能测试

#### 问题 J1: 缺少并发提交流量负载测试
**优先级**: P2 (低)
**状态**: ⚠️ 待办
**解决方案**: 创建 `tests/performance/test_concurrent_submissions.py` 文件。

---

## 测试覆盖率目标

| 模块 | 上一版本 | 当前 | 目标 | 状态 |
|---|---|---|---|---|
| 后端/提交 | 85% | 85% | 90% | ⚠️ 接近目标 |
| 后端/管理 | 80% | 80% | 85% | ⚠️ 接近目标 |
| 后端/WebSocket | 0% | 85% | 80% | ✅ 超出目标 |
| 后端/Simnode 客户端 | 0% | 90% | 80% | ✅ 超出目标 |
| 后端/数据库 | 0% | 85% | 85% | ✅ 达到目标 |
| 后端/录像 | 75% | 75% | 85% | ⚠️ 需完成 F1 |
| 后端/集成 | 20% | 60% | 70% | ⚠️ 接近目标 |
| 前端/管理 | 30% | 50% | 70% | ⚠️ 正在改善 |
| 前端/提交 | 40% | 60% | 70% | ⚠️ 正在改善 |
| 前端/回放 | 0% | 40% | 60% | ⚠️ 需要努力 |
| 安全 | 0% | 90% | 80% | ✅ 超出目标 |
| **总计** | **~40%** | **~65%** | **>=80%** | ⚠️ 正在改善 |

---

## 优先级排序 (待办事项)

### P1 (重要，需尽快修复)
1. ⚠️ **问题 B1**: 并发会话设置测试
2. ⚠️ **问题 F1**: 损坏文件容错测试
3. ✅ **问题 A1**: 部分覆盖于安全测试中，但需完善

### P2 (优化项)
1. ⚠️ **问题 B2**: 状态机边界测试
2. ⚠️ **问题 I1**: 前端单元测试
3. ⚠️ **问题 J1**: 性能负载测试

---

## 剩余工作

要达到 80% 的总覆盖率，需要完成以下工作：
1.  添加损坏 JSONL 文件的测试 (F1) - 1 个用例
2.  添加并发会话设置的测试 (B1) - 1-2 个用例
3.  添加状态机边界测试 (B2) - 3-4 个用例
4.  添加前端单元测试 (I1) - 10+ 个用例
5.  添加性能测试 (J1) - 3-4 个用例
